spring:
  jpa:
    hibernate:
      ddl-auto: update
      database: MYSQL
  datasource:
    url: jdbc:mysql://localhost:3306/northwind?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver


server:
  port: 8080
  servlet:
    context-path: /northwind
  tomcat:
    uri-encoding: UTF-8